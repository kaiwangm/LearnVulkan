add_executable(sandbox)
aux_source_directory(./ SANDBOX_SRC)
target_sources(sandbox PRIVATE ${SANDBOX_SRC})
target_link_libraries(sandbox PUBLIC engine SDL2)
target_include_directories(sandbox PUBLIC ${ROOT_DIR})